import 'package:flutter/material.dart';
class PrivacyPage extends StatelessWidget { const PrivacyPage({super.key});
  @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Privacy information')),body:ListView(padding:const EdgeInsets.all(20),children:const [Text('Your data in this app',style:TextStyle(fontSize:20,fontWeight:FontWeight.bold)),SizedBox(height:12),Text('This version stores profile details, goals, medications, nutrition, water, sleep, activity preferences, and reminder settings locally on this device. Medication and health information is sensitive; avoid sharing your device or screenshots unnecessarily.'),SizedBox(height:16),Text('Notifications',style:TextStyle(fontWeight:FontWeight.bold)),Text('Device notifications are used for reminders. You can change reminder settings at any time.'),SizedBox(height:16),Text('AI Assistant',style:TextStyle(fontWeight:FontWeight.bold)),Text('The current assistant uses local development guidance. Do not enter urgent medical information or rely on it for diagnosis.'),SizedBox(height:16),Text('Future cloud sync',style:TextStyle(fontWeight:FontWeight.bold)),Text('Firebase/cloud synchronization is not active yet. If added later, this screen will be updated to explain cloud storage and account controls.'),SizedBox(height:16),Text('This is app information, not legal advice.',style:TextStyle(fontStyle:FontStyle.italic))])); }
